<ui:UiPage x:Class="RinsTrap.UI.Elements.Settings.Pages.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:RinsTrap.Resources"
      xmlns:dmodels="clr-namespace:RinsTrap.UI.ViewModels.Settings"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:StatisticsViewModel, IsDesignTimeCreatable=True}"
      Title="StatisticsPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <DockPanel>
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Statistics" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="A quick view of your Roblox playtime." Margin="0,4,0,0" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <ui:Button DockPanel.Dock="Right" Content="Refresh" Icon="ArrowClockwise24" Command="{Binding RefreshCommand}" VerticalAlignment="Center" />
        </DockPanel>

        <TextBlock Text="{Binding RangeSummary}" Margin="0,20,0,0" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <UniformGrid Columns="4" Margin="0,8,0,0">
            <ui:Card Margin="0,0,6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Today" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding TodayPlaytime}" Margin="0,6,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Margin="6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Last 7 days" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding WeekPlaytime}" Margin="0,6,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Margin="6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="All time" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding TotalPlaytime}" Margin="0,6,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Margin="6,0,0,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Sessions" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding SessionCount}" Margin="0,6,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
        </UniformGrid>

        <Grid Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Card Grid.Column="0" Margin="0,0,6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Games played" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding GamesPlayed}" Margin="0,6,0,0" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Grid.Column="1" Margin="6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Active days" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding ActiveDays}" Margin="0,6,0,0" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Grid.Column="2" Margin="6,0,0,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Most played" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding MostPlayedGame}" Margin="0,6,0,0" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </ui:Card>
        </Grid>

        <TextBlock Text="Last 7 days" Margin="0,22,0,8" FontSize="20" FontWeight="SemiBold" />
        <ui:Card Padding="8">
            <ItemsControl ItemsSource="{Binding Last7Days}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid MinHeight="38">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Label}" VerticalAlignment="Center" />
                            <ProgressBar Grid.Column="1" Height="8" Maximum="86400" Value="{Binding Seconds}" VerticalAlignment="Center" Margin="8,0" />
                            <TextBlock Grid.Column="2" Text="{Binding Duration}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ui:Card>

        <TextBlock Text="{x:Static resources:Strings.Common_Notifications}" Margin="0,22,0,8" FontSize="20" FontWeight="SemiBold" />
        <ui:Card Padding="14">
            <StackPanel>
                <DockPanel Margin="0,0,0,12">
                    <ui:ToggleSwitch DockPanel.Dock="Right" IsChecked="{Binding NotifySessionEnd, Mode=TwoWay}" />
                    <StackPanel>
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_NotifySessionEnd_Title}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_NotifySessionEnd_Description}" Margin="0,3,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </DockPanel>
                <DockPanel>
                    <ui:ToggleSwitch DockPanel.Dock="Right" IsChecked="{Binding EnablePlaytimeReminder, Mode=TwoWay}" />
                    <StackPanel>
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_Reminder_Title}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_Reminder_Description}" Margin="0,3,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </ui:Card>
    </StackPanel>
</ui:UiPage>
