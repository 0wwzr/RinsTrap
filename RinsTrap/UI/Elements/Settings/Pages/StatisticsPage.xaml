<ui:UiPage x:Class="RinsTrap.UI.Elements.Settings.Pages.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:RinsTrap.Resources"
      xmlns:dmodels="clr-namespace:RinsTrap.UI.ViewModels.Settings"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:StatisticsViewModel, IsDesignTimeCreatable=True}"
      Title="StatisticsPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,24">
        <DockPanel>
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Statistics" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Review your Roblox playtime by date and game." Margin="0,6,0,0" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <ui:Button DockPanel.Dock="Right" Content="Refresh" Icon="ArrowClockwise24" Command="{Binding RefreshCommand}" VerticalAlignment="Center" />
        </DockPanel>

        <ui:Card Margin="0,22,0,0" Padding="16">
            <StackPanel>
                <TextBlock Text="Date range" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="Choose a period, then press Apply to update the totals and game list." Margin="0,6,0,14" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                <StackPanel Orientation="Horizontal">
                    <DatePicker Width="170" SelectedDate="{Binding FromDate, Mode=TwoWay}" SelectedDateFormat="Short" />
                    <TextBlock Text="to" Margin="12,0" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <DatePicker Width="170" SelectedDate="{Binding ToDate, Mode=TwoWay}" SelectedDateFormat="Short" />
                    <ui:Button Content="Apply" Icon="Checkmark24" Command="{Binding ApplyDateRangeCommand}" Margin="12,0,0,0" />
                </StackPanel>
                <TextBlock Text="{Binding RangeSummary}" Margin="0,14,0,0" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </ui:Card>

        <UniformGrid Columns="4" Margin="0,16,0,0">
            <ui:Card Margin="0,0,6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Today" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding TodayPlaytime}" Margin="0,8,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Margin="6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Selected range" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding SelectedRangePlaytime}" Margin="0,8,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Margin="6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="All time" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding TotalPlaytime}" Margin="0,8,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Margin="6,0,0,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Sessions" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding SessionCount}" Margin="0,8,0,0" FontSize="22" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
        </UniformGrid>

        <Grid Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Card Grid.Column="0" Margin="0,0,6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Games in range" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding GamesPlayed}" Margin="0,8,0,0" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Grid.Column="1" Margin="6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Active days" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding ActiveDays}" Margin="0,8,0,0" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
            <ui:Card Grid.Column="2" Margin="6,0,0,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="Average session" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{Binding AverageSession}" Margin="0,8,0,0" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Card>
        </Grid>

        <TextBlock Text="Daily playtime" Margin="0,26,0,10" FontSize="20" FontWeight="SemiBold" />
        <ui:Card Padding="10">
            <ItemsControl ItemsSource="{Binding Days}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid MinHeight="38" Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="84" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding DateLabel}" VerticalAlignment="Center" />
                            <ProgressBar Grid.Column="1" Height="8" Maximum="86400" Value="{Binding Seconds}" VerticalAlignment="Center" Margin="12,0" />
                            <TextBlock Grid.Column="2" Text="{Binding Duration}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ui:Card>

        <TextBlock Text="Games" Margin="0,26,0,10" FontSize="20" FontWeight="SemiBold" />
        <ui:Card Padding="10">
            <StackPanel>
                <TextBlock Text="No games recorded in this date range." Margin="4,4,4,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasGames}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <ItemsControl ItemsSource="{Binding Games}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid MinHeight="46" Margin="4,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="78" />
                                    <ColumnDefinition Width="110" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1" Text="{Binding Duration}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <TextBlock Grid.Column="2" Text="{Binding Sessions}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <TextBlock Grid.Column="3" Text="{Binding LastPlayed}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ui:Card>

        <TextBlock Text="{x:Static resources:Strings.Common_Notifications}" Margin="0,26,0,10" FontSize="20" FontWeight="SemiBold" />
        <ui:Card Padding="14">
            <StackPanel>
                <DockPanel Margin="0,0,0,14">
                    <ui:ToggleSwitch DockPanel.Dock="Right" IsChecked="{Binding NotifySessionEnd, Mode=TwoWay}" />
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_NotifySessionEnd_Title}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_NotifySessionEnd_Description}" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </DockPanel>
                <DockPanel>
                    <ui:ToggleSwitch DockPanel.Dock="Right" IsChecked="{Binding EnablePlaytimeReminder, Mode=TwoWay}" />
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_Reminder_Title}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_Reminder_Description}" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </ui:Card>
    </StackPanel>
</ui:UiPage>
