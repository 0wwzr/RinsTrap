<base:WpfUiWindow x:Class="RinsTrap.UI.Elements.Dialogs.ThemePreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:RinsTrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Theme Preview"
        Width="400" Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Theme Preview" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

        <Border Grid.Row="1" Background="{DynamicResource ApplicationBackgroundBrush}" BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <StackPanel>
                <TextBlock Text="Sample Text" FontSize="16" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock Text="Secondary text content" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0" />
                <Button Content="Sample Button" Margin="0,12,0,0" HorizontalAlignment="Left" />
                <CheckBox Content="Sample Checkbox" Margin="0,12,0,0" />
                <Border Background="{DynamicResource ControlStrongFillColorDefaultBrush}" CornerRadius="4" Padding="8" Margin="0,12,0,0">
                    <TextBlock Text="Card Content" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </Border>
            </StackPanel>
        </Border>

        <Button Grid.Row="2" Content="Apply" HorizontalAlignment="Right" Margin="0,12,0,0" Click="ApplyButton_Click" />
    </Grid>
</base:WpfUiWindow>
