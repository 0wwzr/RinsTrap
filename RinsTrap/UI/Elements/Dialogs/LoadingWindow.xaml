<base:WpfUiWindow x:Class="RinsTrap.UI.Elements.Dialogs.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:RinsTrap.UI.Elements.Base"
        mc:Ignorable="d" 
        Title="Loading"
        Width="300"
        Height="150"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{DynamicResource ApplicationBackgroundBrush}" CornerRadius="8" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <ui:TitleBar Grid.Row="0" Title="RinsTrap" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" ShowClose="False" />
            
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
                <ui:ProgressRing Width="48" Height="48" IsIndeterminate="True" Foreground="{DynamicResource AccentFillColorDefaultBrush}" Margin="0,0,0,16" />
                <TextBlock x:Name="LoadingText" Text="Loading..." FontSize="14" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </StackPanel>
            
            <TextBlock Grid.Row="2" x:Name="DetailText" Text="" FontSize="11" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,12" Visibility="Collapsed" />
        </Grid>
    </Border>
</base:WpfUiWindow>